import {StyleSheet} from 'react-native';

export const fadeTheme = StyleSheet.create({
  stContainer: {
    justifyContent: 'center',
    alignItems: 'center',
  },
  stActivite: {
    position: 'absolute',
  },
});
